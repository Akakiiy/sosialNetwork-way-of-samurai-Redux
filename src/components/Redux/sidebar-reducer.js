let initialState = {
    friends: [
        {id: 1, name: 'Ryan Gosling'},
        {id: 2, name: 'Anton'},
        {id: 3, name: 'Alex'},
    ],
};

const sidebarReducer = (state = initialState, action) => {
    return state;
};

export default sidebarReducer;